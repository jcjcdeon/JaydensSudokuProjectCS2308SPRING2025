#pragma once

// Basic backtracking solver
bool solveBasic(int board[9][9]);

// Efficient solver (placeholder — could be the same as basic for now)
bool solveEfficient(int board[9][9]);

// Validate that the current puzzle is a valid Sudoku board
bool validatePuzzle(int board[9][9]);
